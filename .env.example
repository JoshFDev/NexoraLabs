PORT=3000
MONGO_URI=mongodb://localhost:27017/nexoralabs
JWT_SECRET=clave_secreta_desarrollo
# Orígenes permitidos por CORS (separados por coma):
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
# Correos (Gmail con contraseña de aplicación):
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=tu_correo@gmail.com
# SMTP_PASS=tu_correo_de_aplicacion
# Abstract Email Reputation (valida si el correo existe al registrarse):
# ABSTRACT_API_KEY=tu_api_key_de_abstract